There should be a bot or process to call and create new live prompts and mesh them.

for example - when the init occurs, the 'process bot' can look at the request and decide when prompts to initiate given the input.
this yields many processes to run as `clam cli` and self-graph the process.